import styled from 'styled-components';

export const Small = styled.ImageBackground`
  width:100%;
  aspect-ratio: ${props => props.ratio}
`;


export const Origin = styled.Image`
    width:100%;
    aspect-ratio: ${props => props.ratio}
`